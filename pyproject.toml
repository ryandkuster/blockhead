[project]
name = "blockhead"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ryandkuster", email = "ryan.kuster@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "polars==1.35.0",
    "matplotlib>=3.10.7",
    "numpy==2.3.4",
    "cyvcf2>=0.31",
]

[project.scripts]
blockhead = "blockhead.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
